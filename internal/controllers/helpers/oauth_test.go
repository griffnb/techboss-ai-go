package helpers

import (
	"testing"
)

func TestGetUserInfo(_ *testing.T) {
	/*
		info, err := GetUserInfo("google", "")
		if err != nil {
			t.Error(err)
		}

		log.PrintEntity(info)

		result, err := VerifyAccessToken("google", "")
		if err != nil {
			t.Error(err)
		}

		log.PrintEntity(result)
	*/
}
